[defaults]
inventory = inventory.ini
roles_path = roles
host_key_checking = True
retry_files_enabled = False
stdout_callback = yaml
# Operator supplies the Vault password with --ask-vault-pass or
# --vault-password-file (see README). Do not commit a password file.

[ssh_connection]
pipelining = True
